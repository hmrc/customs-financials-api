<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <logger name="uk.gov" level="WARN"/>

    <logger name="services" level="${logger.services:-INFO}"/>
    <logger name="controllers" level="${logger.controllers:-INFO}"/>
    
</configuration>
